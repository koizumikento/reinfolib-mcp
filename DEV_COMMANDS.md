# 🛠️ 開発コマンド一覧 - reinfolib-mcp

このドキュメントでは、VSCodeでの開発効率を向上させるためのコマンドとショートカットを説明します。

## 📋 コマンドパレット (`Ctrl+Shift+P`)

### 🧪 テスト関連

| コマンド | 説明 | ショートカット |
|---------|------|----------------|
| `🧪 すべてのテストを実行` | 全テストケースを実行 | `Ctrl+Shift+T` |
| `🧪 カバレッジ付きテスト実行` | カバレッジレポート付きテスト | `Ctrl+Shift+C` |
| `🧪 現在のファイルをテスト` | 開いているファイルのテストのみ実行 | `Ctrl+Shift+U` |
| `🧪 失敗したテストのみ再実行` | 前回失敗したテストを再実行 | - |

### 🔍 コード品質チェック

| コマンド | 説明 | ショートカット |
|---------|------|----------------|
| `🔍 Ruff: 全体チェック` | プロジェクト全体のリンティング | `Ctrl+Shift+R` |
| `🔧 Ruff: 自動修正実行` | 修正可能なエラーを自動修正 | `Ctrl+Shift+F` |
| `🔧 Ruff: 現在のファイルをチェック` | 現在のファイルのみチェック | - |
| `🔧 Ruff: 現在のファイルを自動修正` | 現在のファイルのみ自動修正 | - |
| `🏗️ MyPy: 型チェック実行` | 型チェックを実行 | `Ctrl+Shift+Y` |

### 🎨 フォーマット

| コマンド | 説明 | ショートカット |
|---------|------|----------------|
| `🎨 Black: コードフォーマット` | Blackでコードフォーマット | - |

### 🚀 サーバー起動

| コマンド | 説明 | ショートカット |
|---------|------|----------------|
| `🚀 MCPサーバー起動 (stdio)` | MCPサーバーをstdioで起動 | - |
| `🚀 MCPサーバー起動 (HTTP)` | MCPサーバーをHTTPで起動 | `Ctrl+Shift+M` |

### 📦 プロジェクト管理

| コマンド | 説明 | ショートカット |
|---------|------|----------------|
| `📦 プロジェクトビルド` | uvでプロジェクトをビルド | - |
| `🔄 開発環境セットアップ` | 開発依存関係を同期 | - |

## 🎯 デバッグ設定 (`F5` キー)

### デバッグ構成一覧

| 構成名 | 説明 |
|--------|------|
| `🧪 現在のテストファイルをデバッグ` | 現在開いているテストファイルをデバッグ実行 |
| `🧪 すべてのテストをデバッグ` | 全テストをデバッグモードで実行 |
| `🚀 MCPサーバー起動 (デバッグ)` | MCPサーバーをデバッグモードで起動 |
| `🚀 MCPサーバー起動 (HTTP デバッグ)` | HTTPモードでMCPサーバーをデバッグ |
| `🔧 現在のPythonファイルを実行` | 現在のPythonファイルを実行 |
| `📋 CLIコマンドテスト` | CLIコマンドのデバッグテスト |

## ⚙️ 自動実行機能

### ファイル保存時の自動実行

- **コードフォーマット**: Blackで自動フォーマット
- **インポート整理**: isortで自動整理
- **Ruff修正**: 修正可能なエラーの自動修正

## 📁 推奨拡張機能

以下の拡張機能が自動的にお勧めされます：

### 必須拡張機能

- **Python** (`ms-python.python`) - Python開発の基本
- **Ruff** (`charliermarsh.ruff`) - 高速リンター
- **Black Formatter** (`ms-python.black-formatter`) - コードフォーマッター
- **Mypy Type Checker** (`ms-python.mypy-type-checker`) - 型チェック

### 便利な拡張機能

- **Even Better TOML** (`tamasfe.even-better-toml`) - TOML編集サポート
- **Python Docstring Generator** (`njpwerner.autodocstring`) - docstring自動生成
- **Python Indent** (`kevinrose.vsc-python-indent`) - Python特有のインデント
- **Python Test Adapter** (`littlefoxteam.vscode-python-test-adapter`) - テストUI

## 🚀 クイックスタート

### 1. 開発環境セットアップ

```bash
# コマンドパレット -> "🔄 開発環境セットアップ"
# または手動で：
uv sync --group dev
```

### 2. 全テスト実行

```bash
# ショートカット: Ctrl+Shift+T
# または手動で：
uv run pytest tests/ -v
```

### 3. コード品質チェック

```bash
# ショートカット: Ctrl+Shift+R
# または手動で：
uv run ruff check .
```

### 4. 自動修正実行

```bash
# ショートカット: Ctrl+Shift+F
# または手動で：
uv run ruff check . --fix
```

### 5. MCPサーバー起動

```bash
# ショートカット: Ctrl+Shift+M
# または手動で：
uv run python -m reinfolib_mcp.cli --transport http --port 8000
```

## 🐛 デバッグのコツ

### テストデバッグ

1. テストファイルを開く
2. `F5` キーを押す
3. "🧪 現在のテストファイルをデバッグ" を選択
4. ブレークポイントを設定して詳細な動作を確認

### MCPサーバーデバッグ

1. `F5` キーを押す
2. "🚀 MCPサーバー起動 (デバッグ)" を選択
3. 環境変数 `REINFOLIB_API_KEY` が設定されていることを確認

## 🔧 カスタマイズ

### 新しいタスクの追加

`.vscode/tasks.json` を編集して、プロジェクト固有のタスクを追加できます。

### ショートカットの変更

`.vscode/keybindings.json` を編集して、お好みのショートカットに変更できます。

## 📊 パフォーマンス監視

### カバレッジレポート

```bash
# ショートカット: Ctrl+Shift+C
# HTMLレポートは htmlcov/index.html で確認
```

### 型チェック

```bash
# ショートカット: Ctrl+Shift+Y
# 型エラーは「問題」タブで確認
```

---

**💡 ヒント**: VSCodeの統合ターミナルを使用すると、すべてのコマンドを手動で実行することもできます。
